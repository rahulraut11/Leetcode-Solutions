class Solution {
public:
    int minImpossibleOR(vector<int>& nums) {
     sort(nums.begin(),nums.end());
int r=1;
    while(binary_search(nums.begin(),nums.end(),r)){
        r*=2;
    }
    return r;

    }
};