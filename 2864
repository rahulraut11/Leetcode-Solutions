class Solution {
public:
    string maximumOddBinaryNumber(string s) {
        sort(s.rbegin(),s.rend());
        string s1=s.substr(1,s.size()-1);
        s1+="1";
        return s1;

        
    }
};